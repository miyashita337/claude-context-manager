[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --cov=src/hooks
    --cov=src/session-title
    --cov-report=term-missing
    --cov-report=html
