[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --cov=src/analyzer --cov=src/output --cov=src/monitor --cov=src/cache --cov=src/config --cov=src/hooks
    --cov-report=term-missing
    --cov-report=html
